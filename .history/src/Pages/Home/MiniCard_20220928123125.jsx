import {miniCard} from '../../assets/resourse/Resourse.js'



const MiniCard = () => {
  return (
    <div>
      {miniCard.map(()=>{
        return(
          "Card"
        )
      })}
    </div>
  )
}

export default MiniCard
